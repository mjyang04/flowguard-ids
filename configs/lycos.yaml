# Lycos2017 profile. Omitted values use nids.config dataclass defaults.

data:
  dataset: lycos2017
  csv_path: data/raw/lycos.csv
  drop_cols: [flow_id, src_addr, src_port, dst_addr, dst_port, ip_prot, timestamp]
